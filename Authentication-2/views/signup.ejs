<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Sign up page</title>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 flex items-center p-10 flex-col gap-4 text-white">
      <form method="post" action="/create">
        <input
          class="px-2 py-2 rounded-md bg-transparent border-2 border-zinc-800"
          type="text"
          name="username"
          placeholder="Username"
        />
        <input
          class="px-2 py-2 rounded-md bg-transparent border-2 border-zinc-800"
          type="email"
          name="email"
          placeholder="Email"
        />
        <input
          class="px-2 py-2 rounded-md bg-transparent border-2 border-zinc-800"
          type="password"
          name="password"
          placeholder="Password"
        />
        <input
          class="px-2 py-2 rounded-md bg-transparent border-2 border-zinc-800"
          type="number"
          name="age"
          placeholder="Age"
        />
        <input
          class="px-5 py-2 rounded-md bg-blue-500 border-2 border-zinc-800"
          type="submit"
          value="Create Account"
        />
      </form>
    </div>
  </body>
</html>
