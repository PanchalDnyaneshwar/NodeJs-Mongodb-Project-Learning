<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 text-sm inline-block py-3"
          >Logout</a
        >
      </div>
      <h3 class="text-2xl mt-2 mb-4">
        <span class="font-bold text-white" 
          >Hey, <%= user.name %> </span
        ><br />
        <span class="font-light text-white">Welcome to your profile</span>
      </h3>
      <h5 class="mb-4">you can create a post</h5>

      <form action="/post" method="post" class="">
        <textarea
          class="block pt-2 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          placeholder="what`s going on your mind ?"
          name="content"
        ></textarea>

        <input
          type="submit"
          class="block px-3 py-2 bg-blue-500 rounded-md mt-2"
          value="Create new post"
        />
      </form>

      <div class="posts">
        <h3 class="mt-10 text-zinc-400">Your Posts</h3>
        <div class="postcontainer">
          <% user.posts.reverse().forEach(post => { %>
          <div class="post w-1/3 bg-zinc-800 mb-4 rounded-md p-4 mt-4">
            <h4 class="text-sm text-blue-500"><%= user.username %></h4>

            <p class="text-sm text-zinc-400 tracking-tight">
              <%= post.content %>
            </p>

            <div class="btns flex justify-between mt-2">
              <a href="/like/ <%= post._id %>" class="text-blue-500">Like</a>
              <a href="#" class="text-zinc-500">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
